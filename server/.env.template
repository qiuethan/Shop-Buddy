# Shop Buddy Server Environment Variables

# Required API Keys
OPENAI_API_KEY=your_openai_api_key_here
SERPAPI_KEY=your_serpapi_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# Client Configuration (CORS)
CLIENT_URL=http://localhost:3000

# Security Configuration (Optional)
# Comma-separated list of valid API keys for production use
VALID_API_KEYS=

# Additional Security Settings
# These are optional and only used if you want to implement additional security layers

# Trusted proxy settings (if behind a proxy/load balancer)
TRUST_PROXY=1

# Rate limiting overrides (optional - defaults are set in code)
# RATE_LIMIT_GENERAL_WINDOW_MS=900000
# RATE_LIMIT_GENERAL_MAX=100
# RATE_LIMIT_SEARCH_WINDOW_MS=900000
# RATE_LIMIT_SEARCH_MAX=20

# Logging level (optional)
LOG_LEVEL=info